defaults:
  - Crawler
  - _self_

name: CrawlerEval

env:
  numEnvs: ${resolve_default:64,${...num_envs}}

eval:
  evaluate: False 
  linVelStep: 21
  angVelStep: 21 
  headingStep: 8
  swivelStep: 8 
